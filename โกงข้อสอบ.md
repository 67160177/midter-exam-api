WK01 — HTTP & Express Fundamentals

คำถามท้ายบทครอบคลุม HTTP แบบ stateless, lifecycle, HTTP methods และ status codes

ข้อ 1 HTTP เป็น Stateless คืออะไร

HTTP เป็น Stateless หมายความว่า Server จะไม่จดจำสถานะหรือข้อมูลของ Request ก่อนหน้าโดยอัตโนมัติ แต่ละ Request ต้องมีข้อมูลที่จำเป็นสำหรับการประมวลผลด้วยตัวเอง

ผลกระทบต่อ Backend:

หากผู้ใช้ Login แล้ว Server ต้องใช้กลไกเพิ่มเติมในการจดจำตัวตน เช่น

Cookie
Session
Token เช่น JWT

ตัวอย่าง ผู้ใช้ Login สำเร็จแล้ว Request ถัดไปต้องส่ง Token มาด้วย เพราะ Server ไม่ได้จำโดยอัตโนมัติว่าผู้ใช้นี้เคย Login มาก่อน

ข้อ 2 จงเรียงลำดับ HTTP Lifecycle

ลำดับหลักคือ

DNS Resolution — แปลง Domain Name เป็น IP Address
TCP Connection — สร้างการเชื่อมต่อระหว่าง Client และ Server
TLS Handshake — หากเป็น HTTPS จะมีการเข้ารหัส
HTTP Request — Client ส่ง Method, URL, Header และ Body
Server Processing — Server ประมวลผลคำขอ เช่น ตรวจสอบข้อมูลหรือ Query Database
HTTP Response — Server ส่ง Status Code, Header และ Body กลับ
Connection ปิดหรือ Keep-Alive — เลือกปิดหรือใช้ Connection เดิมต่อ

ข้อ 3 GET, POST, PUT, DELETE มี Idempotent หรือไม่
Method Idempotent เหตุผล
GET ใช่ เรียกซ้ำกี่ครั้งก็อ่านข้อมูลเดิม ไม่เปลี่ยนสถานะ
POST ไม่ใช่ เรียกซ้ำอาจสร้างข้อมูลใหม่หลายรายการ
PUT ใช่ ส่งข้อมูลเดิมซ้ำ ผลลัพธ์สุดท้ายเหมือนเดิม
DELETE ใช่ ลบครั้งแรกข้อมูลหาย ครั้งต่อไปก็ไม่มีข้อมูลให้ลบ ผลลัพธ์สุดท้ายยังเหมือนเดิม
ข้อ 4 สร้างข้อมูลใหม่สำเร็จควรใช้ Status Code อะไร

ควรใช้

201 Created

เพราะ HTTP Status Code นี้สื่ออย่างชัดเจนว่า Request สำเร็จและเกิด Resource ใหม่ขึ้น

ไม่ควรใช้ 200 OK เพราะแม้จะหมายถึงสำเร็จเหมือนกัน แต่ 201 Created มีความหมายเฉพาะเจาะจงกว่าและตรงกับสถานการณ์

ข้อ 5 Route GET /api/v1/courses/:courseId

แนวคิดคำตอบ:

รับค่า courseId จาก Path Parameter
ค้นหารายวิชา
หากพบ → 200 OK
หากไม่พบ → 404 Not Found
ข้อ 6 ทำไมควรใช้ Postman แทน Browser อย่างเดียว

Browser เหมาะกับการทดสอบ GET เป็นหลัก แต่ Postman สามารถทดสอบ API ได้ครบกว่า เช่น

GET
POST
PUT
PATCH
DELETE
Request Body
Headers
Authorization
Status Code
Response

ดังนั้น Postman เหมาะกับการทดสอบ Backend API มากกว่า

WK02 — Web Services Standards

เนื้อหาหลักของสัปดาห์นี้คือการเปรียบเทียบ SOAP, REST และ GraphQL

ข้อ 1 SOAP, REST และ GraphQL ต่างกันอย่างไร
ประเด็น SOAP REST GraphQL
ประเภท Protocol Architectural Style Query Language
รูปแบบข้อมูลหลัก XML ส่วนใหญ่นิยม JSON JSON Response
Endpoint Service Operation หลาย Resource Endpoint มักใช้ Endpoint เดียว
ความยืดหยุ่น ต่ำกว่า ปานกลาง สูง
Client เลือก Field ไม่ยืดหยุ่น จำกัดตาม API เลือกเองได้
ข้อ 2 SOAP เหมาะกับระบบแบบใด

SOAP เหมาะกับระบบที่ต้องการ

มาตรฐานเข้มงวด
ความน่าเชื่อถือสูง
Security ระดับสูง
ระบบองค์กรขนาดใหญ่

เช่น

ธนาคาร
ประกันภัย
ระบบองค์กรเก่า (Legacy System)

เพราะ SOAP มีมาตรฐานชัดเจนและรองรับ WS-Security

ข้อ 3 REST มีข้อดีอะไร

ข้อดีของ REST ได้แก่

ใช้งานง่าย
ใช้หลักการ HTTP ที่มีอยู่แล้ว
นิยมใช้ JSON ซึ่งอ่านง่ายและขนาดเล็ก
มีเครื่องมือรองรับจำนวนมาก
เหมาะกับ Web และ Mobile Application
ข้อ 4 Over-fetching และ Under-fetching คืออะไร
Over-fetching

ได้รับข้อมูลมากเกินความจำเป็น

ตัวอย่าง:

Client ต้องการแค่

name

แต่ API ส่งกลับ

{
"id": 1,
"name": "John",
"email": "john@email.com",
"address": "...",
"phone": "..."
}
Under-fetching

ข้อมูลที่ได้ไม่เพียงพอ ต้องเรียก API หลายครั้ง

ตัวอย่าง

GET /students/1
GET /students/1/courses

ต้องเรียกหลาย Endpoint เพื่อประกอบข้อมูลให้ครบ

REST มีโอกาสเกิดทั้งสองปัญหา ส่วน GraphQL ช่วยลดปัญหาเหล่านี้ได้โดยให้ Client ระบุข้อมูลที่ต้องการเอง

ข้อ 5 GraphQL เหมาะกับสถานการณ์ใด

เหมาะเมื่อ

Client ต้องการข้อมูลหลายรูปแบบ
มีหลาย Client เช่น Web และ Mobile
ต้องการลด Over-fetching
ต้องการลด Under-fetching
ต้องการให้ Client เลือก Field เอง
WK03 — RESTful Design & URI Schema

คำถามท้ายบทของ WK03 เน้น Resource Naming, Safe/Idempotent, Status Code และ Content Negotiation

ข้อ 1 ทำไม /getAllStudents และ /deleteStudent/1 ไม่ดี

เพราะ URI ตามหลัก RESTful ควรแทน Resource ด้วยคำนาม (Noun) ไม่ใช่คำกริยา

HTTP Method เป็นตัวบอกการกระทำอยู่แล้ว

ไม่ควร
GET /getAllStudents
GET /deleteStudent/1
ควร
GET /students
DELETE /students/1

ข้อ 2 Safe และ Idempotent ต่างกันอย่างไร
Safe

Request ไม่ควรเปลี่ยนแปลงข้อมูลบน Server

ตัวอย่าง

GET
Idempotent

เรียก Request เดิมซ้ำกี่ครั้ง ผลลัพธ์สุดท้ายเหมือนเดิม

POST
Safe: ไม่ใช่
Idempotent: ไม่ใช่

เพราะเรียกซ้ำอาจสร้างข้อมูลใหม่หลายรายการ

PUT
Safe: ไม่ใช่
Idempotent: ใช่

เพราะส่งข้อมูลเดิมไปแทนที่ซ้ำ ผลลัพธ์สุดท้ายเหมือนเดิม

ข้อ 3 Email ซ้ำควรตอบ Status Code อะไร

ควรตอบ

409 Conflict

เพราะ Request มีรูปแบบถูกต้อง แต่ข้อมูลขัดแย้งกับสถานะปัจจุบันของระบบ

ตัวอย่าง

Email นี้มีอยู่แล้วในระบบ

ไม่ควรใช้ 400 Bad Request เพราะ 400 เหมาะกับกรณีข้อมูลผิดรูปแบบหรือไม่ถูกต้อง เช่น Email ไม่ใช่รูปแบบ Email

ข้อ 4 ออกแบบ URI
ก. ดึงรายชื่ออาจารย์ทั้งหมด
GET /teachers
ข. ดึงรายวิชาของอาจารย์รหัส 5
GET /teachers/5/courses
ค. ลบรายวิชารหัส 200 ออกจากอาจารย์รหัส 5
DELETE /teachers/5/courses/200
ข้อ 5 Content Negotiation คืออะไร

Content Negotiation คือกระบวนการที่ Client และ Server ตกลงรูปแบบข้อมูลที่จะใช้ในการ Response

Client ใช้ Header

Accept

เพื่อระบุรูปแบบที่ต้องการ เช่น

Accept: application/json

หรือ

Accept: application/xml
ข้อ 6 Server ไม่รองรับรูปแบบที่ Client ขอ

ควรตอบ

406 Not Acceptable

หมายถึง Server ไม่สามารถส่งข้อมูลในรูปแบบที่ Client ระบุผ่าน Accept Header ได้

WK04 — Middleware, Basic Security & OWASP
ข้อ 1 Middleware คืออะไร

Middleware คือฟังก์ชันที่ทำงานระหว่าง

Request → Server Processing → Response

สามารถเข้าถึง

req
res
next

และทำงานตามลำดับที่ประกาศไว้

ข้อ 2 หาก Middleware ไม่เรียก next() จะเกิดอะไรขึ้น

หาก Middleware

ไม่เรียก next()
และไม่ส่ง Response

Request จะค้างจนเกิด Timeout

เพราะ Request ไม่ถูกส่งต่อไปยัง Middleware หรือ Route Handler ตัวถัดไป

ข้อ 3 ทำไม Error-Handling Middleware ต้องอยู่ท้ายสุด

เพราะ Express ประมวลผล Middleware ตามลำดับ

Error Handler ต้องถูกวางหลัง Route และ Middleware อื่นทั้งหมด เพื่อให้สามารถรับ Error ที่เกิดขึ้นระหว่างการประมวลผลได้

ข้อ 4 Helmet มีประโยชน์อย่างไร

Helmet เป็น Middleware ที่ช่วยตั้งค่า HTTP Security Headers เพื่อป้องกันความเสี่ยงด้านความปลอดภัยพื้นฐาน เช่น

Clickjacking
Security Misconfiguration บางส่วน
ข้อ 5 CORS คืออะไร

CORS หรือ Cross-Origin Resource Sharing คือกลไกที่ควบคุมว่า Website หรือ Domain ใดสามารถเรียก API จากคนละ Origin ได้

ตัวอย่าง

Frontend:
https://example.com

API:
https://api.example.com

เป็นคนละ Origin จึงต้องมีการกำหนด CORS

ในระบบจริงไม่ควรตั้ง

Access-Control-Allow-Origin: \*

โดยไม่จำเป็น เพราะควรกำหนด Origin ที่อนุญาตให้ชัดเจน

ข้อ 6 Environment Variables มีไว้ทำไม

ใช้เก็บค่าที่ไม่ควรเขียนตายตัวใน Source Code เช่น

Database Password
JWT Secret
API Key
Port
Environment Configuration

ข้อดีคือ

ลดความเสี่ยงข้อมูลลับรั่ว
เปลี่ยน Environment ได้ง่าย
ไม่ต้องแก้ Source Code
WK05 — MySQL Integration & Query Basics

สัปดาห์นี้เน้น Connection Pool, Transaction, ACID และ Parameterized Query

ข้อ 1 Connection Pool คืออะไร และทำไมต้องใช้

Connection Pool คือกลุ่ม Connection ที่สร้างไว้ล่วงหน้าและนำกลับมาใช้ซ้ำ

แทนที่จะเป็น

Request
→ เปิด Database Connection
→ Query
→ ปิด Connection

จะเป็น

Request
→ ยืม Connection จาก Pool
→ Query
→ คืน Connection เข้า Pool

ข้อดีคือ

ลดเวลาในการสร้าง Connection ใหม่
เพิ่มประสิทธิภาพ
รองรับผู้ใช้พร้อมกันได้ดีขึ้น
ควบคุมจำนวน Connection ได้

ข้อ 2 Transaction คืออะไร

Transaction คือกลุ่มคำสั่ง Database หลายคำสั่งที่ต้องถือว่าเป็นงานเดียวกัน

หลักการคือ

สำเร็จทั้งหมด หรือไม่สำเร็จทั้งหมด

ตัวอย่างการโอนเงิน

ลดเงินบัญชี A
เพิ่มเงินบัญชี B

หากทำขั้นตอนแรกสำเร็จแต่ขั้นตอนสองล้มเหลว ระบบต้อง Rollback

ไม่เช่นนั้นข้อมูลจะผิดพลาด

ข้อ 3 ACID คืออะไร
A — Atomicity

ทุกขั้นตอนสำเร็จทั้งหมดหรือย้อนกลับทั้งหมด

C — Consistency

ข้อมูลต้องอยู่ในสถานะที่ถูกต้องตามกฎของระบบ

I — Isolation

Transaction ที่ทำงานพร้อมกันไม่ควรรบกวนกันจนทำให้ข้อมูลผิดพลาด

D — Durability

เมื่อ Commit สำเร็จ ข้อมูลต้องถูกบันทึกถาวร

ข้อ 4 ทำไมต้องใช้ Parameterized Query

เพื่อป้องกัน

SQL Injection

ตัวอย่างแนวคิดที่ไม่ปลอดภัยคือการนำ Input ของ User ไปต่อเป็น SQL โดยตรง

Parameterized Query จะแยก

SQL Structure
Data Value

ออกจากกัน ทำให้ Input ของผู้ใช้ไม่ถูกตีความเป็นคำสั่ง SQL

ข้อ 5 Connection Pool ต่างจาก Transaction อย่างไร
Connection Pool Transaction
จัดการ Connection จัดการความถูกต้องของหลาย Query
เน้น Performance เน้น Data Integrity
Reuse Connection Commit / Rollback
ลดต้นทุนการเชื่อมต่อ ป้องกันข้อมูลอยู่ในสถานะครึ่งกลาง
WK06 — JWT Authentication & RBAC

คำถามท้ายบทของสัปดาห์นี้เน้น Authentication, JWT, bcrypt และลำดับ Middleware

ข้อ 1 Authentication และ Authorization ต่างกันอย่างไร
Authentication

ตอบคำถามว่า

คุณเป็นใคร?

ตัวอย่าง

Login ด้วย Email + Password
Authorization

ตอบคำถามว่า

คุณมีสิทธิ์ทำสิ่งนี้หรือไม่?

ตัวอย่าง

Admin สามารถลบ User ได้
Student ไม่สามารถลบ User ได้

Authentication ต้องเกิดก่อน Authorization

ข้อ 2 ทำไมไม่ควรใส่ Password ใน JWT Payload

เพราะ JWT Payload ไม่ใช่ Encryption

แม้ Token จะถูก Sign เพื่อป้องกันการแก้ไข แต่ Header และ Payload สามารถถูก Decode และอ่านได้

ดังนั้นไม่ควรใส่ข้อมูลอ่อนไหว เช่น

Password
ข้อมูลลับ
Secret

JWT ควรเก็บเฉพาะข้อมูลที่จำเป็น เช่น

{
"id": 5,
"role": "student"
}
ข้อ 3 bcrypt ป้องกันความเสี่ยงอะไร

bcrypt ใช้ Hash Password ก่อนเก็บลง Database

ป้องกันความเสียหายจากกรณี Database รั่ว เพราะ Password จะไม่ถูกเก็บเป็น Plain Text

หลักการคือ

Password
→ bcrypt hash
→ เก็บ hash

ไม่สามารถถอด Hash กลับเป็น Password เดิมโดยตรง

ข้อ 4 ทำไม Login ควรตอบ Error เดียวกันระหว่าง Email ไม่มีและ Password ผิด

เพื่อป้องกันการเปิดเผยว่า

Email ไหนมีอยู่ในระบบ

หากระบบตอบ

ไม่พบ Email

ผู้ไม่หวังดีสามารถทดลอง Email จำนวนมากเพื่อค้นหา Account ที่มีอยู่จริงได้

จึงควรตอบข้อความทั่วไป เช่น

Email หรือ Password ไม่ถูกต้อง
ข้อ 5 ทำไม authenticateToken ต้องมาก่อน authorizeRole

เพราะ

authenticateToken

ทำหน้าที่

ตรวจสอบ JWT
ยืนยันตัวตน
สร้างข้อมูล User เช่น req.user

จากนั้น

authorizeRole

จึงอ่าน

req.user.role

เพื่อตรวจสอบสิทธิ์

ลำดับที่ถูกต้องคือ

Request
↓
authenticateToken
↓
authorizeRole
↓
Route Handler

หากสลับลำดับ authorizeRole จะยังไม่มีข้อมูลผู้ใช้ให้ตรวจสอบ

ข้อสำคัญ 401 vs 403
Status ความหมาย
401 Unauthorized ยังยืนยันตัวตนไม่ได้ เช่น ไม่มี Token หรือ Token ไม่ถูกต้อง
403 Forbidden ยืนยันตัวตนแล้ว แต่ไม่มีสิทธิ์

WK07 — Caching, Performance & Client-Oriented API Design

คำถามท้ายบท WK07 ครอบคลุม Cache, Redis, Pagination, Sorting และ API Versioning

ข้อ 1 ข้อมูลอะไรควรและไม่ควรทำ Cache
ควรทำ Cache
รายชื่อรายวิชา
ข้อมูลสาธารณะที่เปลี่ยนไม่บ่อย

เหตุผล:

ถูกอ่านบ่อย
เปลี่ยนไม่บ่อย
ยอมรับข้อมูลล้าสมัยเล็กน้อยได้
ไม่ควรทำ Cache หรือควรระวังมาก
ยอดเงินในบัญชี
สถานะการชำระเงินแบบ Real-time

เหตุผล:

ข้อมูลต้องถูกต้องและเป็นปัจจุบันสูง หากใช้ข้อมูล Cache ที่เก่าอาจเกิดผลเสียร้ายแรง

ข้อ 2 In-memory Cache กับ Redis ต่างกันอย่างไร
In-memory Cache Redis
อยู่ใน Memory ของ Application เป็นระบบ Cache แยกต่างหาก
เร็วมาก ต้องเชื่อมต่อผ่าน Network
หายเมื่อ Process Restart สามารถตั้งค่า Persistence ได้
ไม่แชร์ระหว่างหลาย Instance แชร์ข้อมูลระหว่างหลาย Instance ได้
เหมาะกับระบบเล็ก เหมาะกับระบบ Scale
ทำไมหลาย Instance ควรใช้ Redis

สมมติ

Server A มี Cache A
Server B มี Cache B

ข้อมูล Cache จะไม่เหมือนกัน

แต่ Redis เป็น Shared Cache

Server A ─┐
Server B ─┼── Redis
Server C ─┘

ทุก Server ใช้ Cache เดียวกัน

ข้อ 3 Cache Invalidation 3 แบบ

1. TTL-based Invalidation

กำหนดอายุ Cache

ตัวอย่าง

TTL = 60 วินาที

เมื่อหมดเวลา Cache ถูกลบหรือหมดอายุ

เหมาะกับข้อมูลที่ยอมรับความล่าช้าได้

2. Explicit Invalidation

เมื่อข้อมูลเปลี่ยน ให้ลบ Cache ทันที

ตัวอย่าง

POST /students

หลังเพิ่ม Student ให้ลบ

students:all

เหมาะกับข้อมูลที่ต้องการให้เห็นการเปลี่ยนแปลงเร็ว

3. Event-based Invalidation

เมื่อเกิด Event บางอย่าง ระบบอื่นที่เกี่ยวข้องรับ Event แล้วลบ Cache

เหมาะกับระบบขนาดใหญ่หรือ Microservices

ข้อ 4 ทำไมต้องจำกัด Pagination Limit

หาก User ส่ง

?limit=1000000

Server อาจต้อง

Query ข้อมูลจำนวนมาก
ใช้ Memory สูง
Response ช้า
Database รับภาระหนัก

ดังนั้นควรกำหนด Maximum Limit เช่น

limit ≤ 100
ข้อ 5 ทำไมชื่อ Column สำหรับ Sort ใช้ Parameterized Query ไม่ได้

Parameterized Query ใช้สำหรับแทน ค่า Data

เช่น

WHERE id = ?

แต่ชื่อ Column เป็นส่วนหนึ่งของ SQL Structure

ตัวอย่าง

ORDER BY ?

ไม่สามารถใช้แทนชื่อ Column ได้ตามปกติ

วิธีป้องกันคือใช้ Allowlist

อนุญาตเฉพาะ

name
created_at
email

หาก User ส่งค่าอื่นให้ Reject

ข้อ 6 URI Path Versioning vs Header Versioning
URI Path Versioning

ตัวอย่าง

/api/v1/students
/api/v2/students

ข้อดี

เข้าใจง่าย
เห็น Version ชัด
ทดสอบง่าย
Debug ง่าย

ข้อเสีย

URI เปลี่ยนเมื่อเปลี่ยน Version
Route อาจซ้ำกันหลาย Version
Header Versioning

ตัวอย่าง

Accept: application/vnd.example.v2+json

ข้อดี

URI สะอาด
Resource URI ไม่เปลี่ยน

ข้อเสีย

มอง Version ไม่เห็นจาก URL
ทดสอบยากกว่า
ซับซ้อนกว่า
